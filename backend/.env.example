# Backend Environment Variables
# Copy this to .env and configure as needed

# Server Configuration
PORT=5001
NODE_ENV=production
CORS_ORIGINS=http://localhost:3000
# Optional: REDIS_URL=redis://localhost:6379
# Optional: STUN/TURN servers can be provided via environment variables
# Optional: Redis Configuration for scaling (uncomment if using Redis)
# REDIS_URL=redis://localhost:6379

# Optional: CORS Configuration (default allows all origins in dev)
# CORS_ORIGIN=http://localhost:3001